name: families_app
description: Application with a list of people and their children

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^7.0.0
  hive: ^2.0.4
  hive_flutter: ^1.0.0
  fluro: ^2.0.3



  #assets
  yalo: ^2.0.1
  yalo_assets:
    path: ./.yalo_assets


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0
  build_runner: ^2.0.3
  hive_generator: ^1.1.0

flutter:

  assets:
    - assets/
    - assets/icons/

  uses-material-design: true
